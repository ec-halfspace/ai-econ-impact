# Ana <> Ken notes

- what do we want from the harnesses? → how should we choose them?
- breaking down by domain, do we see anything different
- if the model fails, why is that → tools/ misunderstandin, cot breaking , what are the failutre model
- define ‘our’ harness, but mention what others use
- focus per domain
- → once the model is trained, how much more capable can we make it
- check medium reasoning more
- $5 per task x3 w 3 scoring rounds
- incl in standup
- React → a couple around for each domain → different models
- 3 short paragraphs for the papers